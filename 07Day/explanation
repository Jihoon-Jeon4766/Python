함수
특정 기능을 수행하는 코드의 집합
def 함수 이름():
  문장

특정 기능
1. 긴 코드를 축약할 때
2. 마법의 모자를 만들고 싶을 때

def 함수이름(매개변수, 매개변수...):
  문장

리턴 기본구조
def 함수 이름():
  문장
  return 자료

정리 : return의 경우에는 값을 반환하는 것을 의미하고
print의 경우에는 전달된 인자를 출력해주는 역할을 합니다.

def 함수 이름(매개변수):
  변수 = 초기값
  #문장
  #문장
  #문장
  return 변수

