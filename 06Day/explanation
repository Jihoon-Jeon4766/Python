리스트[list] 요소 제거
인덱스로 제거 하기 : del, pop()

del : 특정 요소
del 리스트명[인덱스]

pop (): 특정 요소 & 미 입력시 맨 뒤 요소
리스트명.pop(인덱스)

리스트[list] 개념정리
리스트 : 여러가지 자료를 저장할 수 있는 자료형
요소 : 리스트 내부에 있는 각각의 자료
인덱스 : 리스트 내부에서 값의 위치를 표현
-for 반복문과 함께 자주 사용

자료형[list]
리스트[list] + For문
for 반복문과 리스트 사용법
for 변수 in 반복할 수 있는 것:
수행코드

딕셔너리{Dictionary}
키를 사용하여 여러 자료를 저장하는 자료형
{키 : 값}
리스트는 인덱스를 기반으로 값을 저장
딕셔너리는 키를 기반으로 값을 저장

변수명 = {키: 값}

특정 키의 값 출력하기
print(뱐수명 ["키])

키 - 값 추가
딕셔너리 [새로운키] = 새로운 값

update() 설명 : 사전형 자료에 키 - 값을 추가
get() 설명 : 사전형의 값을 반환
keys() 설명 : 사전 형의 모든 키를 반환
values() 설명 : 사전 형의 모든 값을 반환
items() 설명 : 사전향의 모든 키 - 값의 쌍을 튜플로 반환
clear() 설명 : 모든 키 - 값을 삭제하여 빈 사전형 자료만 남김


딕셔너리 : 키를 기반으로 여러 자료를 저장하는 자료형
키 : 딕셔너리 내부에 접근할 때 사용
값: 딕셔너리 내부에 있는 각각의 내용
> 딕셔너리는 for반복문과 함께 자주 사용

String 함수
find(str) 선언 형식 : 문자열에서 특정 문자열을 찾아 해당 문자의 index 값을 반환한다.
count(str) 선언 형식 : 문자열에서 특정 문자열을 찾아 해당 문자열의 수를 반환한다.
lower() 선언 형식 : 문자열에서 영문자를 소문자로 변경하여 반환한다.
upper() 선언 형식 : 문자열에서 영문자를 대문자로 변경하여 반환한다.
strip() 선언 형식 : 문자열의 앞 뒤 공백을 제거한다.
replace 선언 형식 : 문자열에서 특정 문자열을 변경한다.
split(str) 선언 형식 : 문자열의 특정 문자열을 기준으로 분리한다.

