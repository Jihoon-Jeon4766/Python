데이터(data)
데이터 전처리 - 원하는 형태의 데이터로 가공하기

query()   : 행 추출
df[] : 열[변수]추출
sort_values() : 정렬
groupby() : 집단별로 나누기
assign() : 변수 추가
agg() : 통계치 구하기
mergy() : 데이터 합치기(열)
concat() : 데이터 합치기(행)

2. 조건에 맞는 데이터 추출
데이터 전처리 관려 함수
판다스 객체.query()

3. 필요한 변수만 추출
열[변수]추출
데이터 프레임명['변수']

4. 순서대로 정렬하기

5. 파생 변수 추가하기
변수 추가
판다스객체.assign()
