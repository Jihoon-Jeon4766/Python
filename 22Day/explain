k - means
데이터를 K개의 그룹으로 나누오 각 그룹의 중심점을 기준으로 가까운 데이터끼리 묶는 군집화 알고리즘

동작 과정
step1. 데이터셋에 임의의 중심점 k개를 배치
step2. 각 데이터들을 가장 가까운 중심점으로 정하여 군집을 형성
step3. 군집이 형성되면 그 군집 내에서 해당 군집의 중심점을 업데이트
step4. 업데이트한 중심점을 기반으로, 다시 군집을 형성
step5. 더 이상 중심점이 바뀌지 않을 때 까지 step2 ~ step4단계 반복

1. 문제 정의하기
1-1. 문제 상황 이해하기
품종 정보가 없을때 붓꽃 품종을 분할

1-2. 문제 해결에 필요한 정보 살펴보기
필요 정보 1
모델 학습에 사용할 알고리즘은 k-means

필요 정보 2
k-means은 데이터들이 내포하고 있는 특징과 규칙을 찾아내고, 서로 같을까 다를까를 판단해 서 비슷한 데이터끼리 군집화합니다.

필요 정보 3
붓꽃 데이터셋에서 어떤 속성이 있는지와, 붓꽃의 품종 분할에 영향을 미치는 속성을 탐색한 후 해당 데이터를 학습

4. 모델 학습하기
model = KMeans(n_clusters=3)
